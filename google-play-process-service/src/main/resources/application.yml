spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:studs}
    username: ${DB_USER:s412981}
    password: ${DB_PASSWORD:jXUY2dJZrDSNFSok}
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  flyway:
    enabled: true
  jta:
    enabled: true
    transaction-manager-id: ${JTA_TM_ID:google-play-process-service}

server:
  port: ${SERVER_PORT:8080}

logging:
  level:
    org.hibernate.SQL: info

app:
  security:
    jwt:
      secret: ${JWT_SECRET:change-me-in-prod-please-change-me}
      ttl-seconds: ${JWT_TTL_SECONDS:3600}
    users-xml: ${USERS_XML:classpath:security/users.xml}
